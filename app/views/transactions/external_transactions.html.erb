<div class="external-transactions">  
  <div class="banner">
    <%= link_to "Return to your profile", user_path(User.find(session[:user_id])), class: "profile-link" %>
    <h1>External Transactions</h1>
  </div>
  <%= render 'transaction_list' %>
</div>